name: AI Trend Analysis with GitHub Models

on:
  schedule:
    # 每天 7:00 和 12:30 运行（UTC时间，需要减8小时）
    # 7:00 北京时间 = 23:00 UTC (前一天)
    # 12:30 北京时间 = 4:30 UTC
    # 注意：GitHub Actions 定时任务可能延迟 3-30 分钟，这是正常现象
    - cron: "0 23 * * *"  # 每天 7:00 北京时间
    - cron: "30 4 * * *"  # 每天 12:30 北京时间
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write
  models: read  # 必须包含，用于访问 GitHub Models

jobs:
  ai-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 获取完整历史

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify configuration
        run: |
          echo "🔍 检查配置..."
          
          if [ ! -f config/config.yaml ]; then
            echo "❌ 配置文件不存在"
            exit 1
          fi
          
          if [ ! -d output ]; then
            echo "❌ output 目录不存在"
            exit 1
          fi
          
          echo "✅ 配置检查通过"

      - name: Run AI trend analysis with GitHub Models
        env:
          # GitHub Models 配置
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # GitHub Actions 自动提供
          AI_MODEL: "gpt-4o"  # 模型名称
          
          # 通知渠道（可选）
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          WEWORK_WEBHOOK_URL: ${{ secrets.WEWORK_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          BARK_URL: ${{ secrets.BARK_URL }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          
          ENABLE_NOTIFICATION: "true"
        run: |
          echo "🤖 开始 AI 增强热点趋势分析..."
          python analyze_with_github_models.py

      - name: Commit results
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add -A
          git diff --quiet && git diff --staged --quiet || (git commit -m "AI analysis at $(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M')" && git push)

      - name: Summary
        if: always()
        run: |
          echo "✅ AI 分析完成"
          echo "时间: $(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')"
